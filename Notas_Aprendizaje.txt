variables.
string = 'cadena de texto' "" 
number = 45
booleano = true o false

undefined = es una variable que no tiene valor
null = es una variable vacia
Nan = operación que no es un numero

declaración de variables...
(scope)
var =
let = limita el alcance de la variable
const = no pueden cambiar su valor

prompt = es una variable que almacena un dato dado

Operadores en JavaScript.
Aritmeticos. 
son aquellos que toman valores numericos, ya sean variable
o literales y rfetornan a un valor unico

Asignación.
asigna un valor operando de la izquierda basado en el valor
operando de la derecha


Concatenación.
es unir strings.
para realiar concatenación en numeros, se debe generar una
cadena de texto EJ: frases = "" + num1 + num2.
'concat()' es un metodo de cadena, por esa razon es 
necesario que halla un string

Backtikcs.
es la forma de declaración de una variable en un string
se pueden identificar de la siguiente manera ``,
y permiten la concatenación dentro del texto sin necesidad
de darle cierre

Operadores.
operadores de comparación...
representa un valor y lo redtorna a un valor boolean 
comparando retornandolo a un valor bolleano
operadores logicos...
nos devuelven aL resultado a partir del cumplimiento 
de una condición el resultado es boolean y sus 
operandos son valores logicos o se asimilan a ellos

Camel case.
es la manera en la que se escribe o se diferencian las
palabras e una oración con vartias palabras juntas 
diferenciando por mayusculas, donde la primera palabra
esta en minuscula y la segunda se empieza en mayuscula
por ejemplo "holaMundo".

Condicionales.
es una sentencia que nos permite la validacion de una
variable u operación en forma de comparación, que se
realiza entre bloques.